# PORT
PORT=3000

# ENVIRONMENT
NODE_ENV=development

# DATABASE (PostgreSQL connection string)
DATABASE_URL=postgresql://user:password@host:5432/dbname

# REDIS (used for background job queue)
REDIS_URL="rediss://default:your-token@your-host.upstash.io:6379"

# ============================================
# EMAILS
# ============================================
# EMAIL_PROVIDER options:
#   mock   → Just logs to console. No setup needed. (default for dev)
#   gmail  → Sends real emails via Gmail SMTP.
#             1. Enable 2-Step Verification on your Google Account
#             2. Go to: Google Account → Security → App Passwords
#             3. Generate a password for "Mail" → paste below
#   smtp   → Any SMTP (Outlook, Yahoo, Zoho). Fill SMTP_* vars below.
# --------------------------------------------
EMAIL_PROVIDER=mock
EMAIL_FROM=noreply@scheduling-app.com

# Gmail (only needed if EMAIL_PROVIDER=gmail)
GMAIL_USER=your.email@gmail.com
GMAIL_APP_PASSWORD=xxxx xxxx xxxx xxxx

# Generic SMTP (only needed if EMAIL_PROVIDER=smtp)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=you@example.com
SMTP_PASS=yourpassword

# SECURITY
JWT_SECRET=super_secret_key_change_me_in_production
